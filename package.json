{
  "name": "qt-doc-sync",
  "version": "0.1.0",
  "private": true,
  "description": "Feishu document one-way archive skill with TDD-first modular structure.",
  "main": "scripts/sync-archive.js",
  "type": "commonjs",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "test:watch": "node --test --watch tests/*.test.js",
    "start": "node scripts/sync-archive.js"
  }
}
